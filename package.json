{
  "name": "sanctuary",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Sanctuary \u2014 a two-player abstract strategy board game for the browser.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && (vite build || bun build src/main.ts --outdir dist --target browser)",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
